piv0t is a deterministic state-based substitution cipher that is intended as a 'Human-Proof-of-Work' system. Unlike most substitution ciphers that use a static key or a simple shifting table, piv0t recalculates its entire mathematical logic gate for every character processed.
